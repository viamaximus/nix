# PLACEHOLDER - replace with output of:
#   age-keygen -o /tmp/paper-key.txt && age-keygen -y < /tmp/paper-key.txt
# Then PRINT /tmp/paper-key.txt, store it safely, and delete the file.
# The private key is NEVER committed - only this public recipient is.
